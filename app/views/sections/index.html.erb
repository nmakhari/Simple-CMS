<div class="sections index">
    <h2>Sections</h2>
    <%= image_tag("plus_sign.png", size: "11x11", alt: "+")%>
    <%=link_to("Create Section", new_section_path, :class => "action new")%>

        <table class="listing" summary="Section List">
            <tr class="header">
                <th>#</th>
                <th>Section</th>
                <th>Position</th>
                <th>Visible</th>
                <th>Actions</th>
            </tr>
            <%@sections.each{ |section|%>
                <tr>
                <td><%=section.id%></td>
                <td><%=section.name%></td>
                <td><%=section.position%></td>
                <td class="center"><%=status_tag(section.visible)%></td>
                <td class="actions">
                    <%=link_to("Show", section_path(section), :class=>"action new") %>
                    <%=link_to("Edit", edit_section_path(section), :class=>"action edit") %>
                    <%=link_to("Delete", delete_section_path(section), :class=>"action delete") %>
                </td>
            </tr>
        <%}%>
    </table>
</div>

